<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">

    <!-- Cron for Journal Entry to Accounting Transactions Audit  -->
        <!-- <record id="ir_cron_create_accounting_transactions _audit" model="ir.cron">
            <field name="name">Accounting Transactions Audit()</field>
            <field name="active" eval="False"/>
            <field name="user_id" ref="base.user_root"/>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field name="doall" eval="False"/>
            <field name="state">code</field>
            <field name="model_id" ref="model_amazon_removal_order_report_history"/>
            <field name="code">model.auto_import_removal_order_report()</field>
        </record> -->

    </data>
</odoo>
